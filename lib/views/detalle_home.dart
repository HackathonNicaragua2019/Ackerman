import 'package:flutter/material.dart';

class DetalleHome extends StatelessWidget {
  const DetalleHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
